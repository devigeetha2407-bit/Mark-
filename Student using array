#include <stdio.h>

int main() {
    int n;
    int marks[50];

    // Input number of students
    printf("Enter number of students: ");
    scanf("%d", &n);

    // Input marks
    for(int i = 0; i < n; i++) {
        printf("Enter marks of student %d: ", i + 1);
        scanf("%d", &marks[i]);
    }

    // Display marks
    printf("\nMarks of Students:\n");
    for(int i = 0; i < n; i++) {
        printf("Student %d: %d\n", i + 1, marks[i]);
    }

    return 0;
}
